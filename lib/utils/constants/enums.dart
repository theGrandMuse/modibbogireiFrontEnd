/* --
      LIST OF Enums
      They cannot be created inside a class.
-- */

/// Switch of Custom Brand-Text-Size Widget
enum AppRole { admin, user }

enum PublicSectorTier { federal, state, local }

enum AccountCodingConvention { nationalIPSAS, expressbook }

enum GroupChartAccount { unified, separate }

enum TransactionMode { realTime, batch }

enum VoucherNumber { manual, auto }

enum ScheduledVouchers { manual, auto }

enum MonitorCompliance { yes, no }

enum EnforceCompliance { yes, no }

enum FADepreciationMethod { straightLine, reducingBalance }

enum ProductVisibility { published, hidden }

enum DataMode { add, modify }

enum ImageType { asset, network, memory, file }

enum OrderStatus { pending, processing, shipped, delivered, cancelled }

enum PaymentMethods { paypal, googlePay, applePay, visa, masterCard, creditCard, paystack, razorPay, paytm }
