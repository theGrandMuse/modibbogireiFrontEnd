import 'package:modibbogirei/features/screens/pages/populate.dart';
import 'package:flutter/material.dart';

class PopulateMobile extends StatelessWidget {
  const PopulateMobile({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Populate(),
    );
  }
}
